{% extends 'main.jinja2' %}

{% block title %}
    Notifications
{% endblock %}

{% block view %}
    <div id="notifications">
        <a href="{{ app.router['home'].url() }}">
            <img class="logo" src="/static/img/loudspeaker.svg">
        </a>
        <p class="actions">
            <a class="btn fancy" id="create-notification" href="#">
                <i class="icon-plus-circled"></i>
                Create notification
            </a>
        </p>
        <form id="notification-form" action="/api/demo-team/notifications/add" method="POST">
            <select id="notification-type" name="type" class="textinput">
                <option value="Important">Important</option>
                <option value="Lunchtime">Lunch</option>
                <option value="Information">Information</option>
                <option value="Party">Party</option>
            </select>
            <textarea id="notification-text" name="message" class="textinput"></textarea>
            <a id="btn-create-notification" class="btn" href="#">Create notification</a>
        </form>
        <div id="notifications-list"></div>
        <!--<ul>
            {% for item in n_list %}
                <li>
                 <p class="time">{{ item.creation_date.strftime('%-H:%M') }}</p>
                 <p class="message">
                     <i class="icon-fire-station red"></i>
                     <span class="text">
                         <strong>{{ item.type }}</strong>&nbsp;&nbsp;{{ item.message }}
                     </span>
                 </p>
                </li>
            {% endfor %}
        </ul>-->
    </div>
{% endblock %}